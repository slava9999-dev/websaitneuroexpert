# 🧠 NeuroExpert Platform

[![CI](https://github.com/slava9999-dev/websaitneuroexpert/actions/workflows/ci.yml/badge.svg)](https://github.com/slava9999-dev/websaitneuroexpert/actions/workflows/ci.yml)

> AI‑усиленный digital-комплекс: React 18 фронтенд с премиальным видеофоном, FastAPI-бэкенд со SmartContext для памяти диалогов и Telegram-оповещениями.

## ✨ Основные возможности

- 🎬 Профессиональный видеофон с fallback-анимациями и оптимизацией под медленные сети
- 🤖 AI-ассистент на Claude/GPT-4o с памятью в MongoDB и экспоненциальными ретраями
- 📡 Телеграм-уведомления о заявках из формы и AI-чата
- 🧠 Конфигурируемая система услуг (`backend/config/services.json`)
- 🚀 Готовность к деплою на Vercel и локальному запуску через Docker Compose

## 📦 Структура репозитория

```
.
├── api/                     # Vercel serverless entrypoints
│   ├── index.py             # FastAPI приложение для Vercel
│   └── chat.js              # Node.js функция (Gemini fallback)
├── backend/                 # Исходный код FastAPI
│   ├── config/              # Конфигурация услуг и загрузчик
│   ├── memory/              # SmartContext для AI-чата
│   ├── utils/               # Интент-классификатор и вспомогательные функции
│   ├── routes.py            # Маршруты API (chat/contact)
│   └── server.py            # Локальный запуск FastAPI
├── frontend/                # React 18 приложение (CRA + Tailwind)
│   ├── public/              # Статические ассеты (видео, poster)
│   ├── src/
│   │   ├── components/      # UI-блоки лендинга и чат-виджет
│   │   ├── data/            # Моковые данные для витрин
│   │   ├── hooks/           # Пользовательские хуки
│   │   ├── lib/             # Общие утилиты (cn и пр.)
│   │   └── utils/           # Видео-утилиты и вспомогательные функции
│   └── plugins/             # Расширения CRA (health-check, visual edits)
├── docs/                    # Документация (деплой, DevOps, health-check)
├── scripts/                 # Smoke-тесты и вспомогательные скрипты
├── tests/                   # Pytest-юнит тесты
├── config/                  # Примеры конфигураций (gitconfig и т.д.)
├── docker-compose.yml       # Локальная оркестрация (Mongo + сервисы)
└── vercel.json              # Конфигурация развёртывания
```

Подробные инструкции (деплой, DevOps, health-check и т.д.) лежат в папке `docs/`.

## 🚀 Быстрый старт

### Предварительные требования

- Node.js ≥ 20
- Python ≥ 3.11
- Pip / Pipenv / Poetry (по желанию)
- MongoDB (локально через Docker или Atlas)

### Установка зависимостей

```bash
# Frontend
cd frontend
npm install

# Backend
cd ../backend
pip install -r requirements.txt
```

### Переменные окружения

Создайте `.env` в корне проекта или экспортируйте переменные:

```bash
MONGO_URL=mongodb://localhost:27017
DB_NAME=neuroexpert_db
EMERGENT_LLM_KEY=...
CLIENT_ORIGIN_URL=http://localhost:3000
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...
```

Полный перечень переменных смотрите в `docs/DEPLOY.md` и `docs/AI_CHAT_HEALTH.md`.

## 🧑‍💻 Локальная разработка

### Backend (FastAPI)

```bash
uvicorn api.index:app --reload --port 8000
# или standalone запуск
python -m uvicorn backend.server:app --reload --port 8000
```

### Frontend (React)

```bash
cd frontend
npm run start
```

Фронтенд по умолчанию обращается к `/api/*`; используйте `REACT_APP_BACKEND_URL`, чтобы указать иной origin.

### Docker Compose

Для запуска полного стека (frontend + FastAPI + MongoDB) подготовьте `.env` и выполните:

```bash
docker-compose up --build
```

## 🔁 CI/CD

- GitHub Actions workflow: `.github/workflows/ci.yml`
- Триггеры: `push`, `pull_request` (ветка `main`) и ручной запуск `workflow_dispatch`
- **Backend job**: Python 3.11, установка зависимостей из `backend/requirements.txt`, запуск `pytest` (с `PYTHONPATH=api:backend`)
- **Frontend job**: Node.js 20, `npm ci`, `npm run lint`, `npm test -- --watchAll=false`
- Используются кэши `pip` и `npm`, настроена `concurrency`, чтобы отменять параллельные запуски одного ref

## ✅ Тестирование

- **Pytest**: `pytest` (конфигурация в `pytest.ini`, примеры тестов в `tests/`)
- **Scripts**: `scripts/tests/*.py` — smoke-тесты API, Telegram и памяти AI
- **Frontend**: стандартные команды CRA (`npm test`, `npm run test:coverage`)

Перед запуском тестов убедитесь, что MongoDB и необходимые ключи доступны.

## 📚 Документация и полезные материалы

- `docs/DEPLOY.md` — деплой на Vercel
- `docs/AI_CHAT_HEALTH.md` — health-check, мониторинг и отладка AI-ассистента
- `docs/AI_CHAT_FIX_SUMMARY.md` — история фиксов и архитектурные решения
- `docs/DEVOPS.md` — пайплайн CI/CD, секреты и инфраструктура
- `docs/VIDEO_BACKGROUND_IMPLEMENTATION.md` — нюансы реализации видеофона

## 🗺️ Дальнейшие шаги

1. Подключить реальные данные к `backend/config/services.json`
2. Настроить переменные окружения в Vercel/Render
3. Прогнать `scripts/test_health.sh` и smoke-тесты перед релизом
4. Расширить CI (линтинг backend, e2e) и подключить Codecov при необходимости

---

Если нужна помощь с дальнейшей разработкой (AI-ассистент, видеофон, Telegram-бот) — структура проекта готова к доработкам 💪
