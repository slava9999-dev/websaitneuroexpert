# 🚀 Развертывание NeuroExpert на Vercel

## ⚡ Быстрое развертывание

### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd websaitNeuroExpert-main
```

### 2. Установка Vercel CLI
```bash
npm install -g vercel
vercel login
```

### 3. Настройка переменных окружения
```bash
# Необязательно: локально можно создать .env и добавить ключи
# В Vercel задайте переменную окружения:
# - GOOGLE_API_KEY: API ключ Google Gemini
```

### 4. Развертывание
```bash
# Для первого развертывания:
vercel --prod

# Или через GitHub интеграцию в Vercel Dashboard
# Подключите репозиторий к Vercel через dashboard
```

---

## 🔧 Детальная настройка

### Переменные окружения:

| Переменная | Описание | Пример |
|------------|----------|---------|
| `GOOGLE_API_KEY` | API ключ Google Gemini (обязательно) | `AIza...` |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram‑бота (для формы) | `123456:ABC-...` |
| `TELEGRAM_CHAT_ID` | ID чата/канала (для формы) | `7548070478` |

### Структура проекта:

- **`/frontend/`** - React приложение (CRA + craco)
- **`vercel.json`** - Конфигурация развертывания

---

## 🧪 Тестирование deployment

### После развертывания проверьте:

1. **Главная страница**: https://your-app.vercel.app
2. **API endpoints**:
   - `POST /api/gemini` — AI чат (Google Gemini)
   - `POST /api/contact` — отправка формы (нужны TELEGRAM_* переменные)

### Команды для локального тестирования:

```bash
# Фронтенд
cd frontend && npm start
```

---

## 🚨 Важно:

1. **API ключи**: Настройте `GOOGLE_API_KEY` в Vercel (Production/Preview/Development)
2. **Форма**: Для работы `/api/contact` добавьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
3. **Cloudinary**: Для видео фона, уже настроено в коде
4. **Domain**: Настройте кастомный домен neuroexpert.ru в Vercel

---

## 🔄 Обновление

При изменениях в коде:
```bash
git add .
git commit -m "Your changes"
git push origin main
# Vercel автоматически передеплоит
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в Vercel Dashboard
2. Убедитесь в корректности переменных окружения (`GOOGLE_API_KEY`)

Happy deploying! 🎉
