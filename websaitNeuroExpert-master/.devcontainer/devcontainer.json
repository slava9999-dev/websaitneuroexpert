{
  "name": "NeuroExpert DevContainer",
  "dockerFile": "Dockerfile",
  "context": "./.devcontainer",
  "workspaceFolder": "/workspace/websaitNeuroExpert-master",
  "remoteUser": "vscode",
  "features": {
    "git": "latest"
  },
  "postCreateCommand": "\
    cd frontend && npm ci && npm run build && cd ../backend && pip install -r requirements.txt && cd ../frontend/api && pip install -r requirements.txt",
  "forwardPorts": [3000, 8000],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  }
}
